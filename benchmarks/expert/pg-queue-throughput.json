{
  "task": "pg-queue-throughput",
  "rules": [
    "postgres/lock-skip-locked",
    "postgres/lock-short-transactions",
    "postgres/data-pagination",
    "postgres/schema-foreign-key-indexes"
  ],
  "selected_by": "Claude, from the defect table in benchmarks/tasks/README.md, on 2026-09-23, before any model output for this task was seen.",
  "note": "An upper bound for selection: chosen with knowledge of the defects. The sf-expert arm delivers exactly these rules through the same hook as sf-hook, so the two differ only in who selected the rules.",
  "review": {
    "reviewed_by": "Ali",
    "reviewed_on": "2026-09-23",
    "changes": "None. Each of the four rules matches one defect in the task; no missing rule needed.",
    "instructions": "A second developer reviews this list without seeing any model output for the task. Read task.txt, the fixture files and benchmarks/tasks/README.md, then the catalog (compose.py --catalog). Would you hand exactly these rules to a colleague fixing this task? Add or remove IDs as needed, record what you changed and why in 'changes', then fill in reviewed_by and reviewed_on. ab.py refuses paid sf-expert runs until reviewed_by is set."
  }
}
