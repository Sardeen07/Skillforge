---
description: Set the SkillForge mode for this session (only "coding" exists today)
---
SkillForge currently has one mode, "coding". If "$ARGUMENTS" is empty or "coding", use
`--mode coding` when running compose.py. For any other value, say that the mode does not
exist yet and that only "coding" is available.
