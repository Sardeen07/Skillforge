{
  "name": "skillforge-forge",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "python3 -m unittest discover tests -v",
    "compose:demo": "python3 plugin/skills/skillforge/scripts/compose.py --mode coding --subtask \"fix the failing bug\""
  },
  "devDependencies": { "typescript": "^5.0.0", "tsx": "^4.0.0" }
}
